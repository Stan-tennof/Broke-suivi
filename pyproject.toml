[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gta-inventory"
version = "0.1.0"
description = "Discord webhook inventory tracker with PostgreSQL and Streamlit."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
